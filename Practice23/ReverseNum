let num = 2343;
let ld, rev = 0;
while (num > 0) {
  ld = num % 10;
  rev = rev * 10 + ld;
  num = Math.floor(num / 10)
}
console.log("reverse is ", rev)


num = 3456
let sum = 0
while (num > 0) {
  ld = num % 10;
  sum = sum + ld;
  num = Math.floor(num / 10)
}
console.log("Sum of Digit ", sum)
