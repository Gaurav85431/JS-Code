let str = "Hello      Gaurav "
for (let i = 0; i < str.length; i++) {
  if (str[i] === '  ') {
    continue;
  }

  // if  space is more than 1 i.e. 2 than we'll remove that space;
  else {
    
  }
}